Use case: scientific-educational. Generate one finished Japanese textbook diagram. Landscape 16:9, target1920x1080. Match rag-guide: continuous offwhite #F7F6F1 to edges, navy #2B3A4A text, muted blue #5E7E96 arrows, pale blue semantic panels, Noto Sans JP style. A clear concise Japanese title at top, 3-5 meaningful blocks, large readable labels at least equivalent 34px on1920 canvas; generous margins. Use diagrams rather than prose-heavy cards, each connector has correct direction and endpoints. No page number, watermark, shadow, decorative icons, surrounding page frame, tiny footnotes, or TeX commands. Preserve scientific content below, but simplify layout and Japanese text; this is a redraw, not literal rendering of TeX. Labels in affirmative Japanese when meaning permits. Add a short key sentence only if it helps. Explanatory numeric examples are explicitly labelled 例.
Topic: ケース：注文確認AI
Original diagram's semantic specification:
\begin{tikzpicture}[node distance=2mm,every node/.append style={font=\scriptsize}]
    \node[box,minimum width=92mm,minimum height=6mm,inner ysep=.5mm] (adapter1) {入力アダプタ：HTTP API／チャット画面};
    \node[accentbox,minimum width=92mm,minimum height=6mm,inner ysep=.5mm,below=of adapter1] (usecase) {ユースケース：質問受付・事実取得・応答生成};
    \node[box,minimum width=92mm,minimum height=6mm,inner ysep=.5mm,below=of usecase] (domain) {ドメイン：Order・DeliveryEstimate・業務規則};
    \node[accentbox,minimum width=92mm,minimum height=6mm,inner ysep=.5mm,below=of domain] (ports) {ポート：OrderRepository・DeliveryService・ResponseGenerator};
    \node[box,minimum width=92mm,minimum height=6mm,inner ysep=.5mm,below=of ports] (adapter2) {出力アダプタ：PostgreSQL／配送API／LLM API};
    \draw[flow] (adapter1)--(usecase);
    \draw[flow] (usecase)--(domain);
    \draw[flow] (domain)--(ports);
    \draw[flow] (adapter2)--(ports);
  \end{tikzpicture}
MANDATORY scientific corrections and layout guidance:
Ports and adapters diagram distinguish source dependency from runtime. Center ユースケース, domain Order below, interfaces OrderRepository DeliveryService ResponseGenerator on right. HTTP adapter -> use case -> interfaces; output DB/delivery/model adapters -> corresponding interfaces with label 実装する, not dataflow arrows. Title 業務処理が窓口を定め、外部実装を差し替える. Do not imply domain depends on infra.
